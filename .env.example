CHARGEBEE_API_KEY=your_api_key_here
CHARGEBEE_SITE=your_site_name_here

# Optional settings (uncomment to override defaults):
# CHARGEBEE_RATE_LIMIT_RPM=150        # Requests per minute (150=Starter/Test, 1000=Performance, 3500=Enterprise)
# CHARGEBEE_MAX_CONCURRENCY=20        # Max simultaneous API requests (Chargebee allows 50 GET)
# CHARGEBEE_PAGE_SIZE=100             # Records per page (max 100)
# CHARGEBEE_MAX_RETRIES=5             # Max retry attempts on 429/5xx errors
# CHARGEBEE_LOG_LEVEL=INFO            # Log level: DEBUG, INFO, WARNING, ERROR
# CHARGEBEE_OUTPUT_DIR=output         # Output directory for JSON, SQLite, and logs
